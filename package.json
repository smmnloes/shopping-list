{
  "version": "3.2.1",
  "scripts": {
    "build": "npm --prefix backend run build && npm --prefix frontend run build:prod"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "packageManager": "pnpm@9.15.1"
}
